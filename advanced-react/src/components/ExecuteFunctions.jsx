import React from 'react'

const ExecuteFunctions = ({myFunction}) => {
  return (
    <div>
      <button onClick={myFunction}>Clique para executa a função</button>
    </div>
  )
}

export default ExecuteFunctions
